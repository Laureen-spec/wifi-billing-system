@extends('layouts.app')
@section('title', 'Repair POS')
@section('page_heading', 'Repair POS')
@section('content')
@include('repair-pos::partials-style')
<div class="rp-page">
@if(session('success'))<div class="rp-alert">{{ session('success') }}</div>@endif
<div class="rp-hero"><div><h1>Repair POS</h1><p>Repair CRM, device jobs, product inventory, and POS sales as a separate add-on.</p></div><div class="rp-actions"><a class="rp-btn secondary" href="{{ route('repair-pos.jobs.index') }}">Repair Jobs</a><a class="rp-btn secondary" href="{{ route('repair-pos.products.index') }}">Inventory</a><a class="rp-btn" href="{{ route('repair-pos.sales.index') }}">Sales</a></div></div>
<div class="rp-grid"><div class="rp-stat"><span>Customers</span><strong>{{ $stats['customers'] }}</strong></div><div class="rp-stat"><span>Repair Jobs</span><strong>{{ $stats['jobs'] }}</strong></div><div class="rp-stat"><span>Pending Jobs</span><strong>{{ $stats['pending_jobs'] }}</strong></div><div class="rp-stat"><span>Completed</span><strong>{{ $stats['completed_jobs'] }}</strong></div><div class="rp-stat"><span>Products</span><strong>{{ $stats['products'] }}</strong></div><div class="rp-stat"><span>Low Stock</span><strong>{{ $stats['low_stock'] }}</strong></div><div class="rp-stat"><span>Sales</span><strong>{{ $stats['sales_count'] }}</strong></div><div class="rp-stat"><span>Revenue</span><strong>KES {{ number_format($stats['sales_total'], 2) }}</strong></div></div>
<div class="rp-card"><div class="rp-card-header"><h2>Recent Repair Jobs</h2><a class="rp-btn secondary" href="{{ route('repair-pos.jobs.index') }}">View All</a></div><table class="rp-table"><thead><tr><th>Customer</th><th>Device</th><th>Issue</th><th>Status</th><th>Cost</th></tr></thead><tbody>@forelse($recentJobs as $job)<tr><td><strong>{{ $job->customer?->name ?? $job->customer_name ?? 'Walk-in' }}</strong><br><span class="rp-muted">{{ $job->phone }}</span></td><td>{{ ucfirst($job->device_type) }}<br><span class="rp-muted">{{ $job->device_model }}</span></td><td>{{ \Illuminate\Support\Str::limit($job->issue, 70) }}</td><td><span class="rp-badge {{ $job->status }}">{{ ucfirst($job->status) }}</span></td><td>KES {{ number_format($job->final_cost ?: $job->estimated_cost, 2) }}</td></tr>@empty<tr><td colspan="5" class="rp-empty">No repair jobs yet.</td></tr>@endforelse</tbody></table></div>
<div class="rp-card"><div class="rp-card-header"><h2>Recent Sales</h2><a class="rp-btn secondary" href="{{ route('repair-pos.sales.index') }}">View All</a></div><table class="rp-table"><thead><tr><th>Sale</th><th>Customer</th><th>Total</th><th>Method</th><th>Status</th></tr></thead><tbody>@forelse($recentSales as $sale)<tr><td><strong>{{ $sale->sale_number }}</strong><br><span class="rp-muted">{{ $sale->sold_at?->format('Y-m-d H:i') }}</span></td><td>{{ $sale->customer?->name ?? 'Walk-in' }}</td><td>KES {{ number_format($sale->total, 2) }}</td><td>{{ ucfirst($sale->payment_method) }}</td><td><span class="rp-badge {{ $sale->status }}">{{ ucfirst($sale->status) }}</span></td></tr>@empty<tr><td colspan="5" class="rp-empty">No sales yet.</td></tr>@endforelse</tbody></table></div>
</div>
@endsection
